apply plugin: 'java'

repositories {
    google()
    jcenter()
}

dependencies {
    compile 'com.android.billingclient:billing:1.1'
}

task getDeps(type: Copy) {
    delete 'libs'
    from configurations.compile
    into 'libs'
}
